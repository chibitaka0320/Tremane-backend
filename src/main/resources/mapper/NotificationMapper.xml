<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chibitaka.tremane_backend.repository.NotificationRepository">
  <!-- 登録 -->
  <insert id="insert">
    INSERT INTO notifications (user_id, type, message, related_id)
    VALUES (#{userId}, #{type}, #{message}, #{relatedId})
  </insert>

  <!-- 一覧取得(ユーザーID検索, 作成日の新しい順) -->
  <select id="findByUserId">
    SELECT notification_id, user_id, type, message, related_id, is_read, created_at, updated_at
    FROM notifications
    WHERE user_id = #{userId}
    ORDER BY created_at DESC
  </select>

  <!-- 全通知を既読にする -->
  <update id="markAllReadByUserId">
    UPDATE notifications
    SET is_read = true
    WHERE user_id = #{userId}
  </update>

  <!-- 削除（1件） -->
  <delete id="deleteById">
    DELETE FROM notifications
    WHERE notification_id = #{notificationId}
  </delete>

  <!-- 削除（関連リソースID） -->
  <delete id="deleteByRelatedId">
    DELETE FROM notifications
    WHERE related_id = #{relatedId}
  </delete>
</mapper>