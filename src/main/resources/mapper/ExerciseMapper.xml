<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chibitaka.tremane_backend.repository.ExerciseRepository">
  <select id="getExercise">
    SELECT
      exercise_id, parts_id, name, created_at, updated_at
    FROM exercises
    WHERE updated_at > #{updatedAt}
    ORDER BY exercise_id, parts_id
  </select>
</mapper>