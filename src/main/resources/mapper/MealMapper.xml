<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chibitaka.tremane_backend.repository.MealRepository">
  <!-- 食事記録詳細取得(ID、所有者チェックあり) -->
  <select id="findById">
    SELECT
      meal_id, date, user_id, name, created_at, updated_at
    FROM meals
    WHERE meal_id = #{mealId}
      AND user_id = #{userId}
  </select>

  <!-- ユーザー食事記録一覧取得 -->
  <select id="findByUserId">
    SELECT
      meal_id, date, user_id, name, created_at, updated_at
    FROM meals
    WHERE user_id = #{userId}
      AND updated_at > #{updatedAt}
    ORDER BY meal_id
  </select>

  <!-- 食事記録追加更新 -->
  <insert id="upsert" parameterType="com.chibitaka.tremane_backend.entity.MealEntity">
    INSERT INTO meals(meal_id, date, user_id, name, created_at, updated_at)
    VALUES( #{mealId}, #{date}, #{userId}, #{name}, #{createdAt}, #{updatedAt})
    ON CONFLICT (meal_id) DO UPDATE SET
    date=EXCLUDED.date,
    name=EXCLUDED.name,
    updated_at=EXCLUDED.updated_at
  </insert>

  <!-- 食事記録削除 -->
  <delete id="delete">
    DELETE FROM meals
    WHERE meal_id = #{mealId}
      AND user_id = #{userId}
  </delete>
</mapper>
